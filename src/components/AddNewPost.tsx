import React from "react";
import styles from "./AddNewPost.module.css";

const AddNewPost: React.FC = () => {
  return <div className={`card ${styles.addCont}`}>AddNewPost</div>;
};

export default AddNewPost;
